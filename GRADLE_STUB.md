This repository includes a root-level `./gradlew` stub to prevent CI failures when native Android builds are not configured.

- The mobile app in `notes_app_frontend` is intended for Expo web preview.
- If native Android builds are needed, generate the Android project (e.g., `npm run prebuild:android`) and update CI accordingly.
