#!/usr/bin/env bash
# Stub gradle wrapper for CI environments where native Android build is not configured.
# This script exits successfully to avoid failing CI when gradle is invoked unintentionally.
echo "Gradle wrapper stub: Android build is not configured in this container. Skipping."
exit 0
